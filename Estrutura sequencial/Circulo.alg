algoritmo "circulo"

var
    r, area : real
inicio
   escreval("Digite o valor do raio do circulo: ")
   leia(r)
   area <- Pi * r^2
   escreval("A area do circulo é equivalente a : " ,area)
fimalgoritmo
